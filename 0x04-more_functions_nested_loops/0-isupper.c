#include "main.h"
#include <stdio.h>

/**
 * _isupper - if c is upper
 *@c: input for alphabet
 * Return: 1 if it upper
 */
int _isupper(int c)
{
	char c;

	if (c >= 65; c <= 90)

		return (1);
	else
		return (0);
}
