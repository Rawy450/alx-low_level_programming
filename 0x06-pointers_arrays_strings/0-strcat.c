#include<main.h>
/**
  * *_strcat - function that concatenates two strings
  *
  * @dest: input1
  * @src: input2
  * Return: always 0
  */
char *_strcat(char *dest, char *src)
{
	char s1[98] = "Hello";
	char s2[] = "world \n";
	char *pre;

	if (s1 != '\0')
		ptr = _strcat(s1, s2);
	printf("%s", ptr);
	return (0);
}

