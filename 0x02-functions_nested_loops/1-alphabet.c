#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int void print_alphabet(void)
{

	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(str[ch]);
	_putchar("\n");
	return (0);
}
